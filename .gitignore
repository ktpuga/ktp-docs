# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Claude Code local state (settings, worktrees, task locks).
# Our conversations live in ~/.claude/projects and were never in any repo;
# this keeps the per-repo scratch out too.
.claude/
